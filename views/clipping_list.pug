extends layout

block content
  h1= title
  
  ul
  each clipping in clipping_list
    li 
      a(href=clipping.url) #{clipping.name} 
      | (#{clipping.lifespan})

  else
    li There are no clippings.
